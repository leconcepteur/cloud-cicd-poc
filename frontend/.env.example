# API Configuration
# Leave empty or set to /api/v1 to use the Vite proxy (recommended for development)
# Set to full URL for production builds (e.g., https://api.example.com/api/v1)
VITE_API_URL=

# Environment
VITE_ENV=development
